import { Typography } from "@mui/material";

function Dashboard() {
  return (
    <div>
      <Typography variant="caption" color="#fff">
        Dash
      </Typography>
    </div>
  );
}

export default Dashboard;
